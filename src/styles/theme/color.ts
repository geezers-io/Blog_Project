import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#ffc107',
      main: '#ffa000',
      dark: '#ff6f00',
    },
    secondary: {
      main: '#ffb300',
    },
    error: {
      main: '#DA1E28',
    },
  },
});
